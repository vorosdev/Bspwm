#!/usr/bin/env bash

XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
CHROME_USER_FLAGS_FILE="$XDG_CONFIG_HOME/flags.conf"

[[ -f $CHROME_USER_FLAGS_FILE ]] && {
  USER_FLAGS="$(cat $CHROME_USER_FLAGS_FILE | sed 's/#.*//')"
}

/usr/bin/google-chrome-stable $@ $USER_FLAGS
